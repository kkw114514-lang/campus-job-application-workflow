# 夜间模式：睡前确认，授权后按队列执行

目标体验：你休息时，AI 在已授权范围内继续网申；醒来查看提交结果、证据和需要本人处理的问题。

当前状态：作者报告使用 Kimi Blueprint/Automation 每 3 小时触发执行，并提供了浏览器、短信及本地脚本配置。仓库提供执行规范与交接模板，不附后台调度器；尚未提供整晚无人干预的逐次结果和成功率，不承诺所有网站全自动。

## 开启前

- 先在白天完整验证一条申请，确认浏览器、登录、验证码渠道、填写回读和成功证据均可用。
- 睡前确定具体待投申请、附件版本、调剂等意愿及提交授权。将每条申请的 authorization 写入队列；需 AI 提交时使用 authorized 模式。模式本身不代表授权。
- 在本地保存一次夜间任务的起止时间（含时区）、申请 ID 白名单、总申请上限、总执行时间、费用或额度上限及停止条件；没有明确数值不要启动。夜间各轮共用累计预算，不因定时触发而清零。
- 确认客户端支持所需后台执行，电脑供电、网络、浏览器会话与短信同步可用。电脑睡眠、锁屏及客户端关闭的影响必须在实际环境测试；不能假设休眠后仍能操作。
- 只使用一个任务执行队列。调度须保证上次未结束时不启动第二个实例；如果无法保证互斥，就不开启无人值守。队列中的 in_progress 不是锁。

## 每次触发执行什么

1. 核对当前时间、授权范围、累计预算、停止标记与是否存在另一执行者；不满足条件则退出。
2. 先检查 submission_unknown，只核查官方记录，不直接重新提交。接手 in_progress 前确认旧执行已结束并核实实际页面状态。
3. 仅从白名单内的 ready 申请中选择；本轮仍受规则.md 的最多 3 条或 30 分钟约束。
4. 在权限和能力范围内完成填写、验证、回读和六查，再按已有授权提交。缺少事实、新增重要选择或必须本人操作时保存 blocked，继续其他合格申请。
5. 提交后立即核验和保存证据。不能确认结果则记 submission_unknown；禁止将点过按钮视为成功。
6. 更新队列、历史、恢复点与夜间累计预算。本轮结束后由外部调度决定下一次触发；不要用无界循环绕过预算。
7. 无可行动申请、到结束时间、预算耗尽或用户停止时结束，不自行补充新的未授权岗位。

## 结果汇总

早间汇总应包含：本次已核实成功的申请及证据、结果待核实、需本人处理、未开始申请，以及实际耗时和可获得的额度消耗。没有数据的费用写未知，不估算成事实。

## 无人值守实测记录（待补充）

| 日期与时段 | AI/插件版本 | 授权申请数 | 官方确认成功数 | 结果未知数 | 需本人处理数 | 人工介入次数 | 运行中断与原因 |
|---|---|---|---|---|---|---|---|

只写实际结果，不能用单次验证码通过、定时任务启动或表单草稿代替整晚投递成功。公开记录不含个人身份、验证码或完整申请凭证。
