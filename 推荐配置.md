# 推荐配置：Kimi 与 Codex 两条实测路线

记录依据：作者于 2026-09-24 提供的使用配置。以下网站为作者报告的使用案例，不代表本次发布逐站复测；未提供各网站对应验证类型，不做推定。第三方产品的内置功能与版本可用性以实际客户端为准。

## 先选一条路线

| 你使用的助手 | 浏览器路线 | 短信路线 | 定时配置记录 |
|---|---|---|---|
| Kimi | WebBridge 驱动真实 Chrome，可选 Playwright 脚本 | 作者环境内置 iMessage MCP | 作者报告每 3 小时触发 |
| Codex 本地客户端 | Playwright + 独立 Chrome 持久会话 | 单独配置第三方 iMessage MCP | 本地记录每 4 小时触发，核查时已暂停 |

两条路线共用本仓库的材料、队列、核查和证据规则。选一条跑通即可，不需要同时安装，也不能同时操作同一申请。下面的 Codex 路线依据本地配置、脚本和历史任务核查；Kimi 路线依据作者提供的配置。

## 路线一：Kimi

### 哪些需要安装

| 组件 | 作者使用版本 | 作用与报告案例 | 安装方式/仓库提供情况 |
|---|---|---|---|
| kimi-webbridge | 1.11.6 | 控制真实 Chrome 填表、提交；广发、兴业、AQUMON、Kimi BYOJ | 浏览器扩展及对应的助手接入；按官方指南安装，仓库不捆绑 |
| iMessage MCP | 作者所用 Kimi 环境内置，未提供版本 | 获取短信验证码；作者报告用于移动、浦发登录，另有本地 chat.db 读取路径 | 在作者环境无需另装；新用户先确认客户端能力与短信授权，仓库不附短信读取实现 |
| Playwright | npm 包，未提供具体版本 | 用独立浏览器 profile 运行网站脚本；作者报告用于建行、移动等流程 | 可选 Node 依赖，不是浏览器插件；仓库不附逐公司 tools/*.cjs 脚本 |
| ocr.swift | 本地 Swift 脚本 | 使用 macOS Vision 识别图片中的文本，供允许的图形验证码流程使用 | 已附 tools/ocr.swift，需 macOS 与可用 Swift 环境；不处理滑块或点选图片 |
| Blueprint/Automation | 作者所用 Kimi 环境内置，未提供版本 | 作者配置为每 3 小时触发执行 | 在对应客户端内配置；仓库不创建或启用任务，也不提供常驻调度器 |

**首推顺序：先跑通 Kimi + WebBridge 的一条申请；有短信需求再开通相应渠道；有脚本需求再安装 Playwright；最后配置夜间执行。** 不需要把所有组件都装上才能开始。

### 安装入口

- [Kimi 浏览器扩展官方指南](https://www.kimi.ai/help/kimi-webbridge/kimi-webbridge-introduction)：目前文档名称为 Kimi Browser Extension（原 Kimi WebBridge），含浏览器商店和 Kimi Work 接入步骤。1.11.6 是作者实测版本，不代表最新版本或必须降级安装的版本。
- [Playwright 官方安装说明](https://playwright.dev/docs/intro)：仅在需要本地网站脚本时使用。首次安装后记录实际版本，方便复现。
- iMessage 与 Blueprint/Automation：按所用 Kimi 客户端中的可用入口配置。未确认所有版本均内置，不提供推测的安装命令或入口路径。

## 路线二：Codex 本地客户端

核查日期：2026-09-24。核心组合是 **Codex + Playwright + 独立 Chrome 会话 + iMessage MCP + 自动任务**。这条已发现的历史路线没有使用 WebBridge 作为必要组件；浏览器脚本也不是 Playwright MCP 服务器。

| 组件 | 查到的配置 | 作用与安装说明 |
|---|---|---|
| Codex 本地客户端 | 历史任务可运行本地脚本、操作浏览器并调用 MCP | 执行流程与核查；不将历史模型选择作为必需配置 |
| Playwright | 当前原运行目录 1.62.1；另一个交接包 1.63.0 | Node 依赖，控制系统 Chrome；版本是文件核查结果，不保证等于每次投递当天版本 |
| Chrome 独立会话 | 原 session.cjs 使用持久 profile，默认 headless，可切换 headed | 保持登录并操作表单；需可持续运行的本地进程，不依赖用户日常浏览器资料目录 |
| iMessage MCP | 本地包 @wyattjoh/imessage-mcp 0.4.2 | 第三方、单独配置，**不是 Codex 内置短信能力** |
| Deno / Node.js | 核查当天分别为 2.9.7 / 24.19.0 | 运行短信服务和浏览器脚本；并非声明最低版本要求 |
| 自动任务 | 历史“夜间继续求职投递”配置为每 4 小时，核查时 PAUSED | 在客户端另行设置；下载本仓库不会启用任务 |
| 辅助脚本 | 本地另有 workflow.py、compact.cjs、usage-audit.py | 队列、批量填表及用量统计；这些历史脚本未随本仓库发布，不能直接引用为现成工具 |

### 安装与配置顺序

1. 使用能运行本地脚本、读写工作区的 Codex 客户端，按实际环境授予任务需要的访问权限。不能将一份提示词视为权限授权，也不要求全局关闭审批或隔离。
2. 按 [Playwright 官方说明](https://playwright.dev/docs/browsers) 安装 Node 依赖并准备浏览器。作者历史脚本使用系统 Chrome 的 `channel: 'chrome'`；使用捆绑 Chromium 时须相应调整配置。当前仓库没有提供原 session.cjs 或逐网站脚本，需由执行助手在本地建立并测试，不能把此说明理解为一键安装包。
3. 为浏览器设置独立的、持久的本地 profile；同一目录同时只允许一个实例。原环境曾用临时目录，不建议新用户照抄该路径。不要公开浏览器资料或会话令牌。
4. 需要短信登录时，按 [iMessage MCP 上游说明](https://github.com/wyattjoh/imessage-mcp) 准备 macOS、Deno 与短信同步，确认本次短信在 Mac“信息”中可见。按系统提示授予所需访问权限，明确授权验证码读取范围。
5. 将 iMessage MCP 加入 Codex 的用户配置。核查到的历史接法是由 Deno 运行本地源码入口 `packages/imessage-mcp/mod.ts`，不是安装同名浏览器扩展。配置位置与分层规则见 [OpenAI 官方配置说明](https://learn.chatgpt.com/docs/config-file/config-basic)。不直接复制他人的整份 config.toml；只添加所需 MCP 配置，并使用自己的真实安装路径。
6. 白天先跑通浏览器启动、普通表单填写与回读、经授权的短信查询和一条真实申请的成功核验，再配置 [夜间模式](夜间模式.md)。迁移自另一助手时先停止原任务，避免双重提交。

### 历史记录能确认什么

- Codex 历史任务记录了招商银行、博时、顺丰、民生和中广核的成功提交及官网核验。本次只核查本地历史，没有重新登录网站复核，也不公开个人申请截图。
- 宝可梦申请记录了滑块通过、自动获取短信并完成登录，随后因用量限制中断；**该次不能计为成功提交**。
- 查到了每 4 小时的自动任务配置，不等于已统计整晚零人工介入的成功率。当前暂停状态是核查时快照，不能用于判断其他用户的任务状态。
- 没有把原私人交接包整体上传：它包含个人材料路径和申请进度。本文仅提取组件与配置方法，不包含凭证、短信或完整个人记录。

## 共用可选工具：OCR

macOS 下先检查 `swift --version`，再运行：

```sh
swift tools/ocr.swift /path/to/image.png '^[A-Za-z0-9]{3,6}$'
```

输出第一个符合正则的识别候选；无匹配输出 NO_MATCH，图片读取或识别执行错误返回非零退出码。这是文本识别候选，不是验证通过的证据；输入后仍须检查网站结果。默认模式只筛选 3–6 位字母数字，其他格式应传入对应正则。

本次发布修正了原脚本中“不符合正则也加入候选”的问题。工具不发请求、不登录网站、不读取短信。含验证码的临时图片和输出不应写入公开日志；使用后清理。

## 短信能力

作者提供的 `mcp__imessage__*` 表示其环境的工具名称族，不是通用安装命令。MCP 与本地数据库是两条不同接入方式，使用任何一种都须确认明确授权和当前工具允许范围。仅取目标服务、本次请求时间范围内的验证码；读取失败时不能静默扩大到所有短信。

## 从组件跑通到夜间运行

图形验证码、短信验证码、滑块与点选图片均有作者跑通经验。但组件可用、定时任务存在，不等于已证明整晚所有申请无人值守成功。夜间配置及待补充的验证记录见 [夜间模式.md](夜间模式.md)。
