# 岗位雷达（上游线索发现层）

岗位雷达是工作流的**发现层**：定时扫描公开校招信息源，核实、去重、分级后把合格线索写进投递队列的 `ready`。它只发现和核实线索，**绝不登录、绝不填写、绝不投递**——投递由执行层按 [规则.md](规则.md) 处理。两者构成完整闭环：

```
岗位雷达（发现 → 核实 → 分级 → 进 ready）→ 投递执行（填写 → 回读 → 提交 → 证据）
```

## 运行方式

任何支持定时任务（cron / 自动任务 / 定时 Automation）且能给 AI 下达长提示词的环境都可以跑雷达。作者实测环境：Kimi Work Automation（agent local_conversation），每天 08:43（Asia/Shanghai）运行，早于投递轮询时间，保证投递层醒来时队列里已有新线索。

配置步骤：

1. 复制 [岗位雷达任务提示词.模板.md](岗位雷达任务提示词.模板.md)，把候选人画像等占位符替换为本人信息（该文件属于个人运行数据，不要公开）。
2. 设为**手动触发**，试跑一轮，检查日报和队列写入是否正确。
3. 满意后转为每日定时；建议安排在投递轮询之前。
4. 首次运行若 `output/岗位雷达历史.csv` 不存在，雷达会自建表头。

## 信号源（只读公开免登录页面）

| 优先级 | 来源 | 说明 |
|---|---|---|
| 1 | 牛客网校招页 | 服务端渲染，`curl` 拉页面解析 `window.__INITIAL_STATE__` 中的校招岗位，免登录 |
| 2 | 国聘 iguopin.com | 央企国企校招聚合，公开搜索 |
| 3 | WebSearch | 按方向组合关键词，可用 `site:` 限定公司官网校招页 |

明确**禁止**爬智联、前程无忧、BOSS 等需登录或有反爬的平台；不绕过任何验证。单一来源访问失败一次即换搜索兜底，重试不超过 2 次。

## 分级标准

- **S 级**：硬门槛全过（地点、毕业窗口、在招）＋ 城市前二档 ＋ 方向最对口 → 核实通过后直接进 `ready`
- **A 级**：核心要求有小缺口，或城市在第三档但值得投 → 只进日报，由用户拍板
- **B 级**：擦边、规则复杂、需用户判断 → 只进日报
- **C 级**：明确不符 → 不进入日报正文，仅在历史 CSV 留痕

每天进 `ready` 的新候选 **≤ 5 个**，遵守 [规则.md](规则.md) 的新增候选上限。

## 去重

新线索必须同时满足以下三处都不存在，才算新：

1. 投递队列 `application-state.json`（注意公司别名归一）
2. 共享历史（`grep` 定向查）
3. 雷达历史 `output/岗位雷达历史.csv`（雷达自己推过的线索）

## 产出

- `output/岗位雷达_YYYYMMDD.md`：日报，含新线索表（公司/岗位/方向/城市/官方链接/截止/分级/一句话核实依据）与「建议进 ready」清单
- `output/岗位雷达历史.csv`：本轮全部线索留痕，**只含公开岗位信息，不含任何个人信息**
- 进度记录追加一行雷达摘要（日期/扫描源/新线索数/S 级进 ready 数/丢弃数）

## 红线

- 只读公开页面：不登录、不提交、不绕过验证
- 不虚构 JD 信息：核不到的字段写「未核实」，不得猜
- 二手转载只作线索，必须找到官方来源（官网/官方 ATS/官方校招页）才算核实通过
- 过期帖（发布/更新超过 30 天且官方页已下架）直接丢弃并注明

## 作者实测记录

2026-09-24 首次试跑：14/25 次网络请求，产出 24 条去重新线索，5 条 S 级进 `ready`（含邮箱投递、需 SMTP 授权的边缘案例，雷达如实标注渠道）。后续每日运行结果见本地运行数据，不属于本仓库。
