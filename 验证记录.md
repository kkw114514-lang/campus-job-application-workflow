# 本地验证记录

验证日期：2026-09-24。环境：Darwin，Python 3.9.6。

已验证：
- 三个 Python 工具语法正确。
- 在临时空目录初始化成功，真实队列为空，不导入示例申请。
- 再次初始化保留已有材料内容。
- 空队列通过检查并生成 HTML。
- 队列示例通过结构检查。
- 同一岗位重复记录、已提交缺少证据、未知提交结果缺少恢复点、截止时间没有时区均被检查发现。
- 看板保留未知状态记录，并转义输入中的 HTML 标签。

未验证：真实招聘网站投递、浏览器适配、其他操作系统及页面视觉验收。工具检查不能替代官方记录核验。

## v2.1.2 补充验证

- 新增文档的本地链接检查通过。
- OCR 脚本通过 Swift 类型检查。
- 合成图片 AB1234 的识别、正则不匹配返回 NO_MATCH、无效图片路径非零退出均通过。
- 未测试真实验证码或夜间调度；组件案例依据作者提供的信息。
