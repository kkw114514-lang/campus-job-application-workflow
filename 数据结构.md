# 数据结构与 v2.0 迁移

队列顶层使用 `schema_version: "2.1"`、`mode` 和 `applications`。mode 为 assist（辅助）或 authorized（用户已明确授权的执行模式）。每条申请仍须记录自己的提交授权，模式本身不是授权。

| 字段 | 含义 |
|---|---|
| application_id | 本地唯一且稳定的申请 ID |
| company_id / company | 统一公司标识 / 显示名称 |
| recruitment_id / job_id | 招聘计划标识 / 官方岗位编号或经核实的组合标识 |
| position / city / portal | 岗位全名 / 地点 / 官方链接 |
| deadline | 带时区的 ISO 8601 截止时间；未知为 null，不猜测 |
| verified_at / rules_source | 核实日期 / 志愿与渠道规则的官方出处 |
| status / tier | 状态见规则；S/A/B 可投分级，C 不投 |
| channel | 官网、官方邮箱或其他经核实渠道，不作为跨渠道去重豁免 |
| eval / resume_version | 评估卡路径 / 使用的简历版本 |
| authorization | submit 布尔值、scope 具体范围、confirmed_at 确认时间 |
| owner / recovery | 当前执行者 / 恢复点，进度标记不等于锁 |
| reason / next_action | 当前依据 / 下一步 |
| submitted_at / evidence | 官方确认成功时间 / 非空证据路径字符串数组 |

检查工具只检查必需结构、重复身份和基本状态一致性。它不核实证据真实性、资格或授权内容，也不保证历史完整。

## v2.0 迁移

保留旧文件备份，人工逐条核对后迁移。将 companies 拆成具体岗位申请，明确招聘计划和岗位身份；多个岗位不可保留为一条混合状态。applied 只有证据符合规则后才能改 submitted。旧 evidence 字符串改为路径数组；不要把说明文字混入路径。缺少确认信息的旧记录不能直接 ready，应 blocked 并写恢复条件。

没有提供自动迁移，因为旧版自由文本不足以可靠判断是否同一申请。不要用空的新队列取代历史去重：先迁移并核对历史，再继续提交。
