# 环境搭建

1. 安装 Python 3.9+，在仓库根目录运行 `python3 tools/初始化.py`（部分系统命令为 `python`）。
2. 检查 AI 是否可以操作浏览器、读写工作区及保存截图；能力缺失时按 `兼容性说明.md` 人工补足。
3. 由本人登录目标网站，使用工具支持的浏览器会话。不要在不明环境复制浏览器凭证、账号密码或验证码。
4. 初次试运行先完成一个区块的填写与回读，再逐步扩大到整份申请。
5. 使用看板前重新运行生成命令；它不会自动刷新。

本仓库没有预装 Playwright 或绑定任何 MCP 服务。请依据你实际工具的官方说明配置；各网站控件实现不同，不提供假定可运行的通用投递脚本。

默认不启用定时任务及并行执行。后续加入调度时，需先解决任务互斥、授权范围和未知提交结果核查，不能简单定时重放提示词。
