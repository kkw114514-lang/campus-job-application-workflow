# 兼容性说明

本项目不绑定特定 AI 产品，不保证所有浏览器控制工具都能完成同一网站。使用前应检查实际可用能力。

| 环境 | 使用方式 |
|---|---|
| 只有聊天能力 | 整理材料、评估岗位、生成回答与核查清单；本人填写并维护记录 |
| 可操作浏览器 | 在授权范围内导航、填写、回读与截图；具备相应能力且获得授权时可尝试验证；必要时转本人 |
| 可读写本地文件并运行 Python | 可额外使用初始化、检查与静态看板工具 |

仓库初始化、队列检查和看板脚本仅依赖 Python 3.9+ 标准库，按跨平台方式编写；具体平台仍需实测。另附可选 macOS Vision 文本识别脚本 tools/ocr.swift，需要 Swift 环境；没有附带浏览器适配器、短信工具或逐公司自动化脚本。

首次在一个网站使用时，选一条真实申请试运行到填写回读阶段，确认字段映射、附件与恢复点可靠后再提交。验证环节按下表判断是否可由当前 AI 处理，不承诺耗时或成功率。

分享时只分享干净模板目录或 GitHub 仓库，不分享初始化后的完整工作目录。正式发布前参见 `发布检查.md`。

## 验证能力与人工接管

作者于 2026-09-24 确认，实际使用中跑通过以下四类验证。此记录来自作者反馈，并非本次发布检查对真实网站的复测；组件与部分使用案例已补充至 `推荐配置.md`，逐站点的验证类型与运行结果尚待记录。

| 验证类型 | 已知经验 | 当前使用条件 |
|---|---|---|
| 图形验证码 | 作者确认跑通过 | 当前工具允许并具备识别、输入及结果核验能力 |
| 短信验证码 | 作者确认跑通过 | 用户明确授权短信渠道，仅获取本次目标服务验证码，并在有效会话内使用 |
| 滑块 | 作者确认跑通过 | 当前工具和网站允许正常交互，具备相应操作能力；失败按预算转人工 |
| 点选图片 | 作者确认跑通过 | 当前工具和网站允许识别、点选及结果核验；失败按预算转人工 |
| 扫码、人脸或其他本人确认 | 本轮未补充实测结论 | 按网站要求由本人确认，不能从其他验证经验推定支持 |

工作流不覆盖 AI 工具自身的权限与使用规则。某个助手跑通过，不代表另一个助手也可以执行。保存攻略时应补充站点、核实日期、工具版本、验证类型和结果，不收录验证码或个人信息。
