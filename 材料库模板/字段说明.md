# 字段说明

本文件中的值仅为说明，不能用于真实填报。原模板的提示已从数据值中分离；是否最高学历、是否接受调剂均需本人确认。客观事实与证件冲突时先核实。

`字段确认记录` 可按以下格式记录重要字段：
```json
{"字段": "是否接受调剂", "来源": "本人明确确认", "确认时间": "实际日期", "适用申请": "具体申请 ID"}
```

日期建议在材料库统一存储为 YYYY-MM-DD，填表时按网站要求转换。未知排名保持 null，不把未证实的百分比作为事实。亲属信息仅在目标表单要求时按需补充。
