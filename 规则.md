# 规则（状态机 · 预算 · 六查 · 失败处理）

## 一、状态机

| 状态 | 含义 | 转换规则 |
|---|---|---|
| `ready` | 已核实可投 | 唯一直接行动的状态。新候选进 ready 前必须完成核实（地点/毕业窗口/志愿规则/去重） |
| `blocked` | 暂停 | 必须写明**恢复条件**。仅当恢复条件明确变化时解锁，并在日志注明依据；不得自动转 ready |
| `applied` / `submitted` | 已投递 | 必须有官方成功证据 + 岗位名一致 + 证据截图路径。此后绝不重复投递 |
| `skip` | 放弃 | 写明原因（不匹配/超窗口/用户否决） |

## 二、每轮预算（防止烧额度、防止死磕）

- 每轮：≤3 家公司 或 ≤30 分钟，先到先停。
- 每家：≤15 次浏览器脚本 或 ≤10 分钟。
- 同一失败：最多重试 2 次，且第 2 次必须有**新依据**（换了方法/换了结论/环境变化）。
- 同一环节连续失败 ≥3 次：**标记 blocked 停止自动重试**，写明恢复点，等用户能操作时再人工介入（用户手动登录/扫码后由 AI 接手填写）。
- 到限即保存恢复点、更新队列、换下一家。
- 无 ready 且今天已找过新候选 → 本轮结束。
- 每天新增官方候选 ≤5 个。

## 三、提交前六查（不可省略，逐条打勾）

1. **岗位/城市/毕业窗口匹配**（毕业时间、在读状态逐岗核实，不凭印象）
2. **必填字段都有真实来源**（来源 = 材料库/证明文件/用户最新确认；无来源不填）
3. **共享历史无重复**（`grep` 定向查 `共享历史.md`，同公司同岗位去重）
4. **志愿额度与并行规则已核验**（如某公司 2 志愿、截止前可改 N 次、同公司仅一个渠道等，规则须注明出处）
5. **特殊声明已明确**（身高体重、政治面貌、亲属任职披露、社保参保情况、是否接受调剂等敏感项按用户确认值填）
6. **附件为本人真实材料**（简历版本正确、证件照合规）

## 四、事实优先级

**用户当场最新回答 > 事实补充文件 > 材料库/网申基础信息.json > 旧简历/旧日志**。

冲突时以高优先级为准，并把确认过的事实追加到事实补充文件，避免下轮再问。

## 五、每轮收尾

- 更新 `队列/application-state.json`。
- `进度记录.md` 追加本轮摘要：动作、结果、失败原因、恢复点、下次动作。
- 仅当出现以下情况时，在摘要首行标注：`[成功]`（官方确认投递成功）、`[需用户]`（需本人扫码/人脸/手动登录）、`[故障]`（系统/环境问题）。
- 账号密码只在需要时读点号文件，**禁止写入任何日志、聊天记录、进度记录**。
