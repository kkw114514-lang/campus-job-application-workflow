# 网申执行规则（核心规则唯一来源）

本文件是工作流规范，不是对 AI 的独立授权。实际使用须遵守用户授权范围、工具能力与网站要求。其他文件出现冲突时，修正说明文件，不扩大行动范围。

## 1. 范围与授权

只处理岗位可投性核实、材料整理、网申填写、提交核验与记录。默认辅助模式，由用户最终提交。授权执行模式必须有用户明确授权，记录对应申请、授权范围与确认时间；已有有效授权不重复询问。涉及调剂、城市、薪资或承诺内容的重大变化，应确认更新后的选择。

## 2. 申请身份与去重

一条记录对应一个招聘计划中的岗位申请，用本地唯一 `application_id` 标识。`company_id` 统一公司别名；`recruitment_id` 区分年度、批次和独立招聘计划；`job_id` 使用官方岗位编号，无编号时记录经核实的岗位全名、地点及链接组合。

提交前同时查询真实队列与共享历史，按公司、招聘计划和岗位核实跨渠道重复；不同渠道不代表不同申请。一个公司可以有多条岗位申请，能否同时投由官方志愿、渠道、修改次数及独立计划规则决定。志愿顺序调整不应被当作新申请。

默认同一工作区只有一个执行者。`in_progress` 是进度标记，不是原子锁；人工任务、其他 AI 和定时任务不得同时写队列或操作申请。多人并发需另行实现可靠的锁和原子更新，不能仅靠文本查询。

## 3. 状态机

| 状态 | 含义与下一步 |
|---|---|
| `ready` | 岗位、事实、附件及规则已核实，可以开始填写 |
| `in_progress` | 正在填写或等待最后提交，保存执行者和恢复点；接手前确认旧执行已停止 |
| `blocked` | 缺资料、需要人工验证或技术失败；记录恢复条件，条件变化后重新核实再转 ready |
| `submission_unknown` | 已发起提交但没有可靠结果；只核查官方申请记录、回执或客服结果，不能直接重投 |
| `submitted` | 官方明确成功、岗位对应、证据已保存；不可重复提交 |
| `skip` | 不匹配、用户放弃或过期；写明原因 |

未知结果只有在确认官方已接受后转 submitted；确认未接受且允许再次申请后才转 ready。无法确定时维持未知状态并求助本人。

v2.0 的 `applied` 仅在迁移时核验证据后转为 `submitted`，新记录统一用 submitted。

## 4. 预算与优先级

- 每轮最多 3 条申请或 30 分钟，每条最多 15 次浏览器操作批次或 10 分钟，先到先停。批次指一次有明确目的的浏览器工具调用，禁止用一个无界脚本规避预算。
- 同一技术失败首次尝试后最多重试 2 次，每次须有新依据；三次总尝试仍失败即 blocked。人机验证直接交本人，不纳入自动重试。
- 临近官方截止时间的可投申请优先，再按 S/A/B 和用户偏好排序。C 级不得 ready。
- 每日最多新增 5 个已核实候选；无可行动申请时结束。用户可显式调整预算。
- 到限时保存恢复点。若已发起提交但结果不明，必须标为 submission_unknown。

## 5. 事实与附件

未知值为 null；不能把示例、说明、推断或空值当作本人答案。重要事实记录来源和确认日期。本人意愿（调剂、轮岗、薪资等）以最新明确确认为准；客观事实与证件冲突时保留冲突并核实，不能静默覆盖。

复用答案库中的事实，根据岗位和题目重组表达；不得增加不存在的经历、成果或动机。附件记录版本，上传后确认文件名与本人材料相符。

账号优先由用户在自己的浏览器登录。若使用本地凭证文件，须得到明确授权并妥善保护；不在日志记录密码、会话令牌、短信验证码及不必要的个人字段。

## 6. 填写、回读与六查

每区块填写后从页面读取最终值，与事实来源比对，修正后再次读取。日志记录字段数、检查结果和待确认字段名，不复制身份证、家庭信息等实际敏感值。

提交前逐项记录结果：
1. 岗位、城市、毕业窗口等硬门槛匹配。
2. 必填事实有来源，意愿已明确，所有存疑字段已解决。
3. 队列和共享历史无该申请的已提交或未知结果记录。
4. 志愿额度、修改次数、渠道和并行限制有官方依据。
5. 特殊声明与承诺内容已经核对。
6. 本人附件及版本正确，当前模式与提交授权有效。

保存后可能触发字段联动；最终提交前再核对整份表单的关键字段。

## 7. 证据与恢复

成功须有官方成功提示或申请记录，岗位对应一致，并保存截图或官方回执。若成功提示没有岗位名，补存对应申请详情。记录证据路径、时间和可用的申请序列号。

出现成功提示但证据保存失败时，不重新提交；记录 submission_unknown 及已观察到的提示，补查官方记录并恢复证据后再转 submitted。

恢复点记录页面/URL、完成区块、剩余字段名、阻塞原因、恢复条件和下一步；不保存验证码或假定会话固定时长。恢复后先检查登录与已保存内容，不盲目重填。

## 8. 收尾

每次提交尝试后立即更新队列与共享历史，记录结果或未知状态。每轮追加进度摘要；遇到多文件更新中断，下一轮先对账，优先核实任何可能已发起提交的记录。

只在成功、需本人操作、故障或重要状态变化时主动报告；看板是生成时快照，不是实时监控。
